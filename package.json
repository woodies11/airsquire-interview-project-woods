{
  "name": "airsquire-interview-project-woods",
  "private": true,
  "type": "module",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "wait-on": "^7.0.1",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "mongo": "docker-compose up",
    "mongo:down": "docker-compose down",
    "dev:api": "pnpm --filter airsquire-backend dev",
    "dev:web": "pnpm --filter airsquire-next-js dev",
    "dev": "concurrently -k -n mongo,api,web -c yellow,cyan,magenta \"pnpm mongo\" \"pnpm dev:api\" \"pnpm dev:web\"",
    "build": "pnpm --filter airsquire-backend build && pnpm --filter airsquire-next-js build"
  }
}
