{
  "name": "airsquire-interview-project-woods",
  "private": true,
  "type": "module",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "wait-on": "^7.0.1",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "dev:api": "pnpm --filter airsquire-backend dev",
    "dev:web": "pnpm --filter airsquire-next-js dev",
    "dev:py": "python -m uvicorn apps.pycv.app:app --host 0.0.0.0 --port 4021 --reload",
    "dev": "concurrently -k -n py,api,web -c green,cyan,magenta \"pnpm dev:py\" \"pnpm dev:api\" \"pnpm dev:web\"",
    "build": "pnpm --filter airsquire-backend build && pnpm --filter airsquire-next-js build"
  }
}
